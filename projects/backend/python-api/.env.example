## Python (Flask) API .env.example
# Copy to .env and replace values with real secrets. Do NOT commit real secrets.

FLASK_ENV=development
FLASK_SECRET_KEY=changeme
PORT=5000
DATABASE_URL=postgres://user:password@localhost:5432/dbname
REDIS_URL=redis://localhost:6379
JWT_SECRET=changeme
PORT=5000
