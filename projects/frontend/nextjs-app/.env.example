## Next.js .env.example
# Public vars (exposed to client) must be prefixed with NEXT_PUBLIC_

NEXT_PUBLIC_API_BASE_URL=https://api.example.com
NEXT_PUBLIC_ANALYTICS_ID=

# Server-only secrets (do NOT expose to client)
JWT_SECRET=changeme
